name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Install deploy tool
        run: npm install -g angular-cli-ghpages

      - name: Build
        run: npx ng build --configuration=production --base-href="/lokesh-portfolio-animated/"

      - name: Configure git identity
        run: |
          git config --global user.email "lokeshmawane@gmail.com"
          git config --global user.name "Lokesh Mawane"

      - name: Deploy
        run: npx angular-cli-ghpages --dir=dist/lokesh-portfolio/browser
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}